/**
 * NBT DataVersion for Minecraft 1.21.8 (the target version for this project's curated
 * full-cube block table). Update this alongside fullCubeBlocks.ts if the target version
 * changes. Minecraft's data-fixer-upper tolerates a mismatched DataVersion on load — this
 * only needs to be reasonably close to the actual game version, not byte-perfect.
 */
export const DATA_VERSION = 4440;
